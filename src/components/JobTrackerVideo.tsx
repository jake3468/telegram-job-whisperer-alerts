import React from 'react';

interface JobTrackerAnimationProps {
  className?: string;
  showControls?: boolean;
}

export const JobTrackerVideo: React.FC<JobTrackerAnimationProps> = ({ 
  className = ''
}) => {
  // Component removed - animations disabled for performance
  return null;
};